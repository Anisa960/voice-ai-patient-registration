# Server
PORT=3000

# SQLite file path (defaults to ./patients.db if unset)
DATABASE_PATH=./patients.db

# Only needed if you add a shared-secret check on the /patients routes so
# random internet traffic can't hit your API (see README "Security" note).
# WEBHOOK_SECRET=replace-with-a-random-string
